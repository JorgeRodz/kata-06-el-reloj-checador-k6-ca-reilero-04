<!DOCTYPE html>
<html>
  <head>
    <title>TimeChecker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-dark">
      <div class="container-fluid custom_margin">
        <a href="/"><%= image_tag("foo-corp_logo_white.png", size: "90x84", alt: "Edit Entry") %></a>
      <% if logged_in? %>
        <h1 class="nav_h1">👨‍💻 Admin hub </h1>
      <% else %>
          <h1 class="nav_h1">🕗️ Time Checker </h1>
      <% end %>
      <% if logged_in? %>
        <%= link_to 'Log out' , logout_path, method: :delete,class: 'btn btn-warning' %>
      <% else %>
        <%= link_to 'Admin' , login_path, class: 'btn btn-success' %>
      <% end %>
      </div>
    </nav>
    <%= render 'layouts/flash_msgs' %>
    <div class='container yield_container'>
      <%= yield %>
    </div>
    <footer class="application_footer">
      <p><span><%= image_tag("bright_coders_logo.png", size: "25x20", alt: "Edit Entry") %></span> BrightCoders - Reloj Checador ©️ 2022 </p>
    </footer>
  </body>
</html>
